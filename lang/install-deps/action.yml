name: 'Install Dependencies'
description: 'Install dependencies for a speller build'

inputs:
  sudo:
    description: Requires sudo
    default: "true"
    required: false
  apertium:
    description: Requires apertium
    required: true
  
runs:
  using: 'node12'
  main: 'index.js'
